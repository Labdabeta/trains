#include "tasks.h"

/*
static void sensor_listener(void)
{
	Service();
	int i;
	int parent = MyParentTid();
	int tout = WhoIs("TOUT");
	int tin = WhoIs("TIN");

	for (ever) {
		Putc(tout, 1, 133);

		for (i = 0; i < 10; ++i) {
			int sw = Getc(tin, 1);
			printf("Sensor: %d\n\r");
		}
	}
}
*/

void controller()
{
	Exit();
}
